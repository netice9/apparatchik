version: "2"
services:
  test:
    build:
      dockerfile: Dockerfile.ci
      context: .
    command: ["ginkgo -r -v"]
    entrypoint: ["/bin/sh","-c"]
    volumes:
      - ./test-deps:/test-deps:ro
      - "/var/run/docker.sock:/var/run/docker.sock"
